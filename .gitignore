# package directories
node_modules
jspm_packages

.DS_Dist

# Serverless directories
.serverless

.envrc
.awspublish*
*.swp

# don't track the dist directory as the build scripts should handle this
dist
